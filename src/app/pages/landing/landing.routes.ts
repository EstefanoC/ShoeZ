import { Routes } from '@angular/router';

// Components
import { LandingComponent } from './landing-layout/landing.component';

export const AUTH_ROUTES: Routes = [{ path: '/', component: LandingComponent }];
